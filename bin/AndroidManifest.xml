<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cmu.cs.cs446.wifibuffer"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="edu.cmu.cs.cs446.wifibuffer.WifiBufferService"
            android:process=":remote" >
            <intent-filter>

                <!-- The interfaces supported by the service which we can bind to. -->
                <action android:name="edu.cmu.cs.cs446.wifibuffer.IWifiBufferService" />
                <!-- Used to select the service without explicitly supplying the implementation class. -->
                <action android:name="edu.cmu.cs.cs446.wifibuffer.REMOTE_SERVICE" />
            </intent-filter>
        </service>

        <activity
            android:name="edu.cmu.cs.cs446.wifibuffer.WifiBufferService$Controller"
            android:label="@string/activity_remote_service_controller"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.SAMPLE_CODE" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.cmu.cs.cs446.wifibuffer.WifiBufferService$Binding"
            android:label="@string/activity_remote_service_binding" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.SAMPLE_CODE" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.cmu.cs.cs446.wifibuffer.WifiBufferService$BindingOptions"
            android:label="@string/activity_remote_service_binding_options" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>